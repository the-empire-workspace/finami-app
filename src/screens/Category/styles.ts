import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  root: {
    flex: 1,
    width: '100%',
    height: '100%',
    alignItems: 'center',
  },
  noItemBox: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  noItemText: {
    fontSize: 40,
  },
  newButton: {
    width: '95%',
    height: 90,
    marginTop: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },
  newButtonText: {
    fontSize: 40,
    fontWeight: '700',
  },
})
