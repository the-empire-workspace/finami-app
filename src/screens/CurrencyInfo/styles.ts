import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  root: {
    flex: 1,
    width: '100%',
    height: '100%',
  },
  infoCurrency: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  currencyTitle: {
    fontSize: 22,
    marginVertical: 10,
  },
})
