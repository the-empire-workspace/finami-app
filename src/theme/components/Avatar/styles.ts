import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  logoContainer: {
    width: 150,
    height: 150,
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 100,
    overflow: 'hidden',
    borderWidth: 1,
  },
  logo: {
    width: 144,
    height: 144,
  },
})
